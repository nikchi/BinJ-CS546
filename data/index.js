const userData = require("./user");
const showsData = require("./shows");
const reviewsData = require("./reviews");

module.exports = {
    users: userData,
    shows: showsData,
    reviews: reviewsData
};