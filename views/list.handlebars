<h1 id="the-heading">Stop watching, start BinJing!</h1>
{{#if user }}
<p> Logged in as: <a href="/users/{{user._id}}">{{ user.username }}</a></p>
<a href="/users/logout"> Logout </a>
{{ else }}
<a href="/users/login"> Login </a>
<br>
<a href="/users/new"> Sign Up </a>
{{/if }}
<div class="row">
    <div class="shows-list">
        {{#each shows}}
        {{> show }}
        {{/each}}
    </div>
</div>
