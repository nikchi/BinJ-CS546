<hr>
<div class="review-box">
	<div class="counter">
		<a id="upvote"><i class="em em-clap"></i></a>
		<p id="score">{{ rating }}</p> {{!-- {{Will need to use jquery/ajax to update this number without reloading page}} --}}
		<a id="downvote"><i class="em em-poop"></i></a>
	</div>
	{{#if flagged}}
	<div class="flagged">
		<img src="flagged.jpg">
	</div>
	{{/if}}
	<div class="review-body">
		<h2>{{ title }}</h2>
		<p> Rating: {{rating}} </p>
		<p> Poster: {{poster}} </p>
		<p> {{body}} </p>
	</div>

</div>
</br>